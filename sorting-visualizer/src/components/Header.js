import logo from './logo/logo.jpg';
const Header = () => {
    return (
        <header>
            <img src={logo} alt="LazySort Logo" style={{ height: '50px' }} />
        </header>
    );
};

export default Header;